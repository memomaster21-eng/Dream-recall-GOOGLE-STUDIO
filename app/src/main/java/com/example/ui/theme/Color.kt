package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Palette
val LucidBlue = Color(0xFF9FDBFF)
val DreamIndigo = Color(0xFF121A33)
val DeepSpace = Color(0xFF090B14)
val AuroraPurple = Color(0xFF756BFF)
val MoonWhite = Color(0xFFF5F7FF)

// Semantic Colors
val SuccessGreen = Color(0xFF74E7B9)
val WarningYellow = Color(0xFFFFD37A)
val DangerRed = Color(0xFFFF8B8B)

// Equivalents for existing code compatibility
val DeepBlack = DeepSpace
val DeepNavy = DreamIndigo
val SoftCyan = LucidBlue
val DimLavender = AuroraPurple
val WarmWhite = MoonWhite
val CardBackground = DreamIndigo
val GrayText = Color(0xFFA1AABB) // Fallback for UI

val GlassWhite = Color.White.copy(alpha = 0.04f)
val GlassBorder = Color.White.copy(alpha = 0.08f)
